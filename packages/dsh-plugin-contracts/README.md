# Relay DSH Plugin Contracts

`@relay/dsh-plugin-contracts` publishes type-only contracts for Workbench views and
terminal providers, plus numeric API-version constants. It is a library, not a DSH
profile layer, and contains no service implementation.
