# Upstream Checkouts

This directory is for local clones of upstream projects used as references.

## DeepSeek Harness

```bash
../scripts/sync-dsh.sh
```

The `deepseek-harness/` directory is ignored by the Relay repository because it is a separate Git repository.
