# KeySync 安装 DSH 与插件管理演示

## 视频定位

本素材面向首次使用 DSH 的用户，演示从 KeySync 一键安装 DSH，到配置预先准备的 DeepSeek 模型服务，再到打开 DSH、通过对话搜索并安装插件的完整流程。

最终视频：`final/keysync-dsh-plugin-search-install-beginner-guide.mp4`

- 时长：1 分 19.8 秒
- 画面：1280 x 720，30 fps，H.264，yuv420p
- 声音：无音轨
- 演示插件：`relay-dsh-plugin-codex` 0.1.2（npm）

## 内容时间线

| 时间 | 内容 |
| --- | --- |
| 00:00-00:21.37 | 在 KeySync 中一键安装 DSH，包含运行时、DSH 与插件管理器的安装结果 |
| 00:21.37-00:33.37 | 选择预先准备的“DeepSeek 教学服务”，绑定并应用到 DSH |
| 00:33.37-00:41.97 | 从 KeySync 点击“打开”，进入 DSH 首页 |
| 00:41.97-01:19.80 | 在 DSH 对话中搜索 `relay-dsh-plugin-codex`，查看结果并完成安装确认 |

第三段与第四段使用 0.4 秒淡化转场，因此章节边界存在短暂画面重叠。

## 素材结构

- `source/`：已有的插件搜索、安装演示源视频副本。
- `raw/`：本次录制的原始分段和未采用录次，保留用于复核。
- `segments/`：剪除等待并规格统一后的四个正式分段。
- `final/`：对外使用的完整拼接视频。
- `qa/`：探针、抽帧、接触表、等待分析、校验和验收报告。

## 录制环境

- KeySync：本地当前开发工作区构建。
- DSH：0.1.0-rc.7。
- DSH 托管 Node.js：22.23.2。
- DSH 插件管理器：0.1.0-rc.2。
- 模型服务：本地教学账号中预先创建的 DeepSeek 类型服务，默认模型为 `deepseek-v4-flash`。

录制使用独立的 KeySync 服务器、状态目录、用户目录和 DSH 安装目录。账号及 API Key 均为本地虚构测试数据，视频和文档不包含可用凭据。

## 剪辑说明

一键安装阶段保留点击、下载、安装进度和成功状态；中间无交互的安装等待以 4 倍速呈现。模型配置与打开 DSH 按真实操作边界裁剪。插件管理段沿用插件项目已提供的正式演示素材，仅做编码规格统一和转场拼接。

自动等待检测器未识别到本次界面中的细粒度鼠标与状态变化，直接使用其建议会误删必要步骤。最终剪辑改用接触表和关键帧逐项人工走查，详细结果见 `qa/QA-REPORT.md`。
